# Harness compatibility tests

Harness profile installation and packed-plugin loading are orchestrated by the
scripts in `tooling/`. Run `pixi run harness-dump` for the development profile
and `pixi run test-packed` for an isolated tarball installation test.
